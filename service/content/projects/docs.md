### projects.json docs (so i dont forget)

```
{
  (proj_title): {
    "description": (desc_string),
    "tags": [(...tags_string)],
    "image": (image_path_relative_to_public),
    "redirect": (url_string),
  }
}
```
